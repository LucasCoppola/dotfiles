-- Personal input settings ported from the pre-Quattro config.
hl.config({
  input = {
    kb_layout = "us",
    kb_options = "caps:escape",
    repeat_rate = 40,
    repeat_delay = 250,
    sensitivity = 0.2,
    touchpad = {
      scroll_factor = 0.4,
    },
  },
})

hl.device({
  name = "logitech-g203-lightsync-gaming-mouse",
  sensitivity = -0.9,
})
